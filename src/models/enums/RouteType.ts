export enum RouteType {
  ADMIN,
  AUTH
}

export enum RouteName {
  DASHBOARD = 'dashboard',
  MARKS = 'marks',
  MODELS = 'models',
  VEHICLES = 'vehicles',
  PROFILE = 'profile',
  SIGN_IN = 'sign-in'
}
