export enum RouteType {
  ADMIN,
  AUTH
}

export enum RouteName {
  DASHBOARD = 'dashboard',
  MARKS = 'marks',
  MODELS = 'models',
  VEHICLES = 'vehicles',
  CLIENT_MESSAGES = 'client-messages',
  USERS = 'users',
  PROFILE = 'profile',
  SIGN_IN = 'sign-in'
}
